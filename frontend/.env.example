# ============================================================
# OpenCode Platform 前端 - 環境變數配置
# ============================================================
# 使用方式: 複製此文件為 .env，然後修改對應的值
# 注意: 前端使用的變數需要以 VITE_ 開頭才能在代碼中訪問
# ============================================================

# 後端 API 配置（會自動從根目錄 .env 讀取）
# 如果需要覆蓋，可以設定以下變數：

# API 服務地址（如果後端在不同機器或端口）
# VITE_API_URL=http://localhost:8000

# 前端開發服務器端口（預設: 5173）
# FRONTEND_PORT=5173
