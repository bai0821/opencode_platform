# ============================================================
# OpenCode Platform - 環境變數配置
# ============================================================
# 使用方式: 複製此文件為 .env，然後修改對應的值
# ============================================================

# ------------------------------------------------------------
# 🔑 必要配置 - 必須設定才能運行
# ------------------------------------------------------------

# OpenAI API Key（用於 LLM 對話）
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Cohere API Key（用於多語言 Embedding，推薦）
# 如果不設定，會使用 OpenAI embedding
COHERE_API_KEY=your-cohere-api-key-here

# ------------------------------------------------------------
# 🌐 服務端口配置
# ------------------------------------------------------------

# 後端 API 端口（預設: 8888）
API_PORT=8888

# 後端 API 綁定地址（預設: 0.0.0.0）
API_HOST=0.0.0.0

# 前端開發服務器端口（預設: 5173，在 vite.config.js 中設定）
# FRONTEND_PORT=5173

# ------------------------------------------------------------
# 🗄️ Qdrant 向量資料庫
# ------------------------------------------------------------

# Qdrant 服務地址（預設: localhost）
QDRANT_HOST=localhost

# Qdrant 服務端口（預設: 6333）
QDRANT_PORT=6333

# 預設 Collection 名稱（預設: rag_knowledge_base）
QDRANT_COLLECTION=rag_knowledge_base

# ------------------------------------------------------------
# 📊 Embedding 配置
# ------------------------------------------------------------

# Embedding 提供者: cohere 或 openai（預設: cohere）
EMBEDDING_PROVIDER=cohere

# Cohere Embedding 模型（預設: embed-multilingual-v3.0）
COHERE_EMBED_MODEL=embed-multilingual-v3.0

# OpenAI Embedding 模型（預設: text-embedding-3-small）
OPENAI_EMBED_MODEL=text-embedding-3-small

# ------------------------------------------------------------
# 🤖 LLM 配置
# ------------------------------------------------------------

# 預設 LLM 模型（預設: gpt-4o）
LLM_MODEL=gpt-4o

# 規劃器使用的模型（預設: gpt-4o-mini）
PLANNER_MODEL=gpt-4o-mini

# ------------------------------------------------------------
# 🔐 認證配置
# ------------------------------------------------------------

# JWT 密鑰（生產環境請務必更換！）
JWT_SECRET_KEY=opencode-super-secret-key-change-in-production

# JWT Token 過期時間（分鐘，預設: 1440 = 24小時）
JWT_EXPIRE_MINUTES=1440

# 預設管理員帳號
ADMIN_USERNAME=admin

# 預設管理員密碼（首次啟動時創建）
ADMIN_PASSWORD=admin123

# ------------------------------------------------------------
# 💰 成本追蹤配置
# ------------------------------------------------------------

# 每日預算上限（美元）
DAILY_BUDGET=10.0

# 每月預算上限（美元）
MONTHLY_BUDGET=100.0

# ------------------------------------------------------------
# 📋 審計日誌配置
# ------------------------------------------------------------

# 日誌保留天數
AUDIT_RETENTION_DAYS=30

# ------------------------------------------------------------
# 🔍 網路搜尋配置（可選）
# ------------------------------------------------------------

# Tavily API Key（更好的搜尋結果）
# TAVILY_API_KEY=your-tavily-api-key

# SerpAPI Key（Google 搜尋）
# SERPAPI_KEY=your-serpapi-key

# ------------------------------------------------------------
# 🔧 Redis 配置（可選，用於快取）
# ------------------------------------------------------------

# Redis 地址（如果不設定，使用本地快取）
# REDIS_HOST=localhost
# REDIS_PORT=6379

# ------------------------------------------------------------
# 🐛 開發配置
# ------------------------------------------------------------

# 是否啟用 Debug 模式（預設: false）
DEBUG=false

# 日誌等級: DEBUG, INFO, WARNING, ERROR（預設: INFO）
LOG_LEVEL=INFO
